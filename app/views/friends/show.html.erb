<p style="color: green"><%= notice %></p>

<div class="fs-4">
  <%= render @friend %>
</div>

<div class="fs-5">
  <%= link_to "Edit", edit_friend_path(@friend), class: "btn btn-secondary link-underline-opacity-0" %> 
  <%= link_to "Back", friends_path, class: "btn btn-secondary link-underline-opacity-0" %>
  <%= button_to "Delete this friend", @friend, method: :delete, class: "btn btn-secondary link-underline-opacity-0 mt-1" %>
</div>

